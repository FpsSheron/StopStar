sova = 0
def setup():
    size(500,500)
def draw():
    circle(random(0,500),random(0,500),random(5,16))
    fill("#F4F511")
def mouseClicked():
    global sova
    if sova == 0:
        sova = 1
        loop()
    elif sova == 1:
        sova = 0
        noLoop()
